#include <stdio.h>

/**
 * main - main is the maind block of code
 *
 * Return: returns 0 when code  runs
 */

int main(void)
{
	char i;

	for (i = 'a' ; i <= 'z' ; i++)
		putchar(i);
	putchar('\n');
	return (0);
}

